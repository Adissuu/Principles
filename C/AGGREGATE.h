#include <stdio.h>

float minf(float *array, int size); // Returns the minimum element of an array.

float maxf(float *array, int size); // Returns the maximum element of an array.

float sumf(float *array, int size); // Returns the sum of all elements in an array.

float avgf(float *array, int size); // Returns the average of all elements in an array.

float pseudo_avgf(float *array, int size); // Return the average of the max + min