(defun reverseList (list)
    (cond 
        ((null list) '())
        (t (append (reverseList (cdr list)) (list (car list))))))

(defun tribonacci-seq (number)
    (setq zero 0)
    (setq zero2 0)
    (setq one 1)
    (cond 
        )
)